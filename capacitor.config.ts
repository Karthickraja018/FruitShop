import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.punitha.fruitshop',
  appName: 'punitha-fruitshop',
  webDir: 'dist'
};

export default config;
